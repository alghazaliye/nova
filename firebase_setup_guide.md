# دليل الحصول على ملف google-services.json

ملف `google-services.json` هو ملف إعدادات يربط تطبيق الأندرويد الخاص بك بخدمات **Firebase** (مثل الإشعارات وقاعدة البيانات الفورية). إليك خطوات الحصول عليه:

---

### الخطوة 1: الدخول إلى منصة Firebase
1.  اذهب إلى [Firebase Console](https://console.firebase.google.com/).
2.  سجل الدخول باستخدام حساب جوجل الخاص بك.

---

### الخطوة 2: إنشاء مشروع جديد
1.  اضغط على **"Add project"** (إضافة مشروع).
2.  أدخل اسماً للمشروع (مثلاً: `NOVA Messenger`).
3.  اتبع الخطوات (يمكنك تعطيل Google Analytics إذا أردت التبسيط) ثم اضغط **"Create project"**.

---

### الخطوة 3: إضافة تطبيق أندرويد للمشروع
1.  في لوحة تحكم المشروع، اضغط على أيقونة **Android** (شعار الرجل الآلي الأخضر).
2.  سيطلب منك **Android package name**. **هام جداً:** يجب أن تكتب الاسم التالي بالضبط كما هو موجود في الكود:
    > `com.nova.messenger`
3.  (اختياري) يمكنك كتابة اسم مستعار للتطبيق.
4.  اضغط على **"Register app"**.

---

### الخطوة 4: تحميل الملف
1.  بعد تسجيل التطبيق، سيظهر لك زر **"Download google-services.json"**.
2.  قم بتحميل الملف على جهازك.

---

### الخطوة 5: ماذا تفعل بالملف؟
*   **للبناء المحلي:** ضعه داخل مجلد `android/NOVAMessenger/app/`.
*   **للبناء السحابي (GitHub):** افتح الملف بالمفكرة (Notepad)، انسخ كل النص الموجود بداخله، وضعه في **GitHub Secrets** تحت اسم `GOOGLE_SERVICES_JSON` كما شرحت لك في الدليل السابق.

---

**ملاحظة:** إذا فقدت الملف مستقبلاً، يمكنك دائماً العودة إلى إعدادات المشروع في Firebase وتحميله مرة أخرى.
