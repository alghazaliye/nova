# دليل بناء تطبيق NOVA Messenger سحابياً عبر GitHub Actions

هذه الطريقة تسمح لك بتوليد ملف APK تلقائياً باستخدام خوادم GitHub دون الحاجة لتثبيت أي شيء على جهازك.

---

### الخطوة 1: إضافة ملف سير العمل (Workflow)
1.  في مستودعك على GitHub، قم بإنشاء مجلد جديد باسم `.github` (إذا لم يكن موجوداً).
2.  داخل مجلد `.github` قم بإنشاء مجلد آخر باسم `workflows`.
3.  قم بإنشاء ملف جديد داخل `workflows` باسم `android_build.yml`.
4.  انسخ محتوى الملف المرفق `github_action_android.yml` وضعه بداخله.

---

### الخطوة 2: إضافة ملف Firebase كـ Secret (ضروري للنجاح)
بما أن ملف `google-services.json` يحتوي على معلومات حساسة، لا نرفعه مباشرة للكود، بل نضعه في "أسرار" GitHub:
1.  افتح ملف `google-services.json` الموجود لديك وانسخ كل محتواه (نص JSON).
2.  في مستودعك على GitHub، اذهب إلى **Settings** > **Secrets and variables** > **Actions**.
3.  اضغط على **New repository secret**.
4.  في خانة الاسم (Name) اكتب: `GOOGLE_SERVICES_JSON`.
5.  في خانة القيمة (Value) الصق محتوى الملف الذي نسخته.

---

### الخطوة 3: تشغيل البناء والحصول على الـ APK
1.  بمجرد رفع الملفات أو عمل Push للكود، سيبدأ GitHub بالبناء تلقائياً.
2.  لمتابعة العملية، اذهب إلى تبويب **Actions** في مستودعك.
3.  بعد انتهاء البناء (يستغرق حوالي 3-5 دقائق)، اضغط على المهمة الناجحة.
4.  ستجد في الأسفل قسم **Artifacts** وفيه ملف باسم `nova-messenger-debug-apk`. قم بتحميله وفكه وستجد ملف الـ APK بداخله.

---

### لماذا هذه الطريقة أفضل؟
*   **لا تستهلك موارد جهازك:** البناء يتم على خوادم GitHub القوية.
*   **تلقائية:** أي تعديل تفعله في الكود (مثل تغيير رابط الـ API) سيتم بناء نسخة جديدة له فوراً.
*   **نظيفة:** تضمن أن البناء يتم دائماً في بيئة نظيفة وموحدة.
