# تقدم المهمة — 2026-08-21 (محدّث)

## مهم: بنية JWT
- **JWT المستخدم العادي (AuthController createSession)**: payload `{'sub': USER_ID, 'iat', 'exp', 'jti'}` — **sub وليس user_id!**
- **JWT الأدمن (AdminOtpController adminApiLogin)**: payload `{'user_id':1, 'role':'admin', 'admin_role':'super_admin', 'iat', 'exp'}` — user_id + role.
- أي كود جديد يتحقق من JWT يجب أن يقرأ `$payload['user_id'] ?? $payload['sub']`.

## حالة الحزمة الاختبارية
- **test_bundle_final.py: 19/19 PASS** ✓
- test_appeals3.py: كانت فاشلة بسبب:
  - login أثناء ban → الآن 403 ACCOUNT_BANNED ✓
  - login أثناء suspend → الآن 403 ACCOUNT_SUSPENDED (رسالة عربية "معلق مؤقتًا حتى...") ✓
  - **المشكلة الحالية: POST /appeals 401 "بيانات الجلسة غير صالحة"** — AppealsController يقرأ user_id لكن payload المستخدم فيه sub فقط.

## إصلاح مطلوب الآن
- AppealsController constructor: `$this->userId = (int)($payload['user_id'] ?? $payload['sub'] ?? 0);`

## ما أُنجز
1. test_bundle_final.py: 19/19 PASS.
2. AdminController.php: suspendUser (is_blocked=1 + suspend_until + kick sessions)، listAppeals، reviewAppeal (approved=auto-unban+is_blocked=0)، createAppeal، adminId() يدعم standalone admin JWT (standaloneAdminId)، userAdmin/subscribeUser أُصلحت الأعمدة (starts_at/expires_at، os/device_name)، unbanUser يلغي التعليق المؤقت.
3. AppealsController.php (جديد): POST/GET /appeals — تحقق JWT مباشر (المحظور قد تُلغى جلسته)، requires is_blocked=1، يمنع duplicate pending، حد 5-1000 حرف.
4. index.php: routes الاعتراضات والتعليق + require_once AppealsController.
5. AuthController.php: ban/suspend check في login (POST /auth/login — 403 ACCOUNT_SUSPENDED/ACCOUNT_BANNED قبل إرسال OTP) + في verifyOtp + loginOrCreateWithoutOtp.
6. device_registrations أعمدة حقيقية: id,user_id,device_uuid,device_name,device_model,platform,os,os_version,app_version,device_fingerprint,fcm_token,is_active,last_seen,created_at,updated_at.
7. user_subscriptions أعمدة حقيقية: id,user_id,plan_id,status,starts_at,expires_at,created_at.

## ملاحظات
- adminApiLogin token: `{"user_id":1,"role":"admin","admin_role":"super_admin"}`.
- admin: admin@nova-messenger.com / 738155861 (id=1, role super_admin id=1).
- suspend hours: 1,6,12,24,72,168,720.
- privacy_settings: 20 حقلًا. search لا يرجع phone (privacy).
- Render URL: nova-wn25.onrender.com (commit 86ada88 live، DB يُمسح مع deploy).
- GitHub: alghazaliye/nova.
- IP الساندبوكس محجوب من Render → smoke test عبر browser_console_exec.
- JWT_SECRET المحلي: فارغ في env (يعمل للإصدار والتحقق بنفس القيمة).
- الخادم المحلي: php -S 0.0.0.0:8080 من /home/ubuntu/nova_new/backend/public (session: default).

## المتبقي
- [ ] إصلاح AppealsController user_id/sub.
- [ ] test_appeals3.py يجب 13/13.
- [ ] المرحلة 5: payment_requests endpoints + admin/plans.php upgrade.
- [ ] المرحلة 6: admin/reports.php ترقية + admin/chats.php + admin/calls.php + admin/monitoring.php.
- [ ] Flutter: flutter analyze (الخصوصية أُنجزت).
- [ ] المرحلة 7: اختبار شامل (test_bundle_final 19/19، test_privacy_api، test_reports_curl).
- [ ] المرحلة 8: commit + GitHub push + Render deploy + smoke test.
- [ ] المرحلة 9: التقرير النهائي.

## ملفات اختبار
- /tmp/test_bundle_final.py (19 اختبارًا)
- /tmp/test_appeals3.py (اعتراضات — يجب إصلاح فحص "معلق مؤقتا" إلى "معلق مؤقتًا" وتوكن /appeals)
- /tmp/test_login_ban.py (ban/suspend في login ✓ يعمل)
- /tmp/test_privacy_api.py، /tmp/test_reports_curl.sh، /tmp/render_smoke_bundle.py، /tmp/seed_permissions.php


## تحديث المرحلة 5 (الباقات) — قيد التنفيذ
- device limit enforcement موجود في DeviceController::register (يرفض 403 إذا تجاوز الحد) ✓.
- payment_requests جدول موجود: id,user_id,plan_id,status,receipt_path,admin_note,reviewed_by,reviewed_at,created_at.
- admin/plans.php رُقي: plan_type (free/verification/premium/pro/custom)، enable_verification checkbox، verification_duration_days، عرض النوع والتوثيق في الجدول، وتفعيل اشتراك مع checkbox "توثيق مستقل".
- admin/plans.php action subscribe يستخدم NOW() لـstarts_at — يجب التحقق من MysqlCompatPdo يحول NOW() ✓.
- **متبقي المرحلة 5**:
  1. admin/plans.php: عند subscribe مع sub_verification → حقل verified_until (أو users.verified_until?) — يجب فحص الأعمدة المتاحة في users لتحديد بنية "توثيق مستقل".
  2. payment_requests endpoints API: POST /subscriptions/request، GET /subscriptions/my، admin endpoints للمراجعة (approve/reject مع receipt).
  3. AdminController subscribeUser: دعم التوثيق المستقل (enable_verification من plan → verified_until = now + plan.verification_duration_days).
  4. plans public endpoint يعيد plan_type+enable_verification.
  5. admin/subscriptions.php: عرض verified_until (توثيق مستقل).
  6. ملاحظة: ban في register: file 16 يطلب منع التسجيل بـphone/email محظور — يجب التحقق من وجوده في AuthController::register.
- **admin/subscriptions.php**: 114 سطر — cancel + تفعيل موجود. يحتاج إضافة verified_until وإشعار قبل الانتهاء (notification في جدول notifications).
- users أعمدة: id,uuid,phone,email,name,username,bio,avatar,status_text,is_verified,is_blocked,... (فحص أعمدة users عند الحاجة).
- test_appeals3.py: 13/13 ✓، test_bundle_final.py: 19/19 ✓.
- admin/plans.php action=subscribe body: INSERT starts_at=NOW() — SQLite OK عبر MysqlCompatPdo.


## تحديث المرحلة 5 (12:40) — الباقات والاشتراكات
### أُنجِز حتى الآن:
1. ✅ database.php: +users.verified_until في migrateMissingColumns
2. ✅ AdminController:
   - subscribeUser: expires_at الصحيح (كان bug: يمرر adminId بدل expires_at + ON DUPLICATE KEY خاطئة) + verified_until مستقل (enable_verification + verification_duration_days)
   - cancelSubscription: يمسح verified_until عند لا اشتراك نشط
   - plansIndex/plansCreate/plansUpdate: دعم plan_type, enable_verification, verification_duration_days
   - userAdmin: يرجع verified_until
3. ✅ schema.sqlite.sql: users.verified_until + plans(plan_type, enable_verification, verification_duration_days) + payment_requests + user_bans + user_appeals + report_attachments
4. ✅ admin/plans.php: الفورم يعرض/يحفظ plan_type + enable_verification checkbox + verification_duration_days

### المتبقي في المرحلة 5:
- [ ] payment_requests endpoints: POST /subscriptions/request (مستخدم يطلب اشتراك) + GET /subscriptions/my + admin approve/reject: POST /admin/payment-requests/{id}/approve + reject
- [ ] admin pages: payment-requests.php (صفحة طلبات الاشتراك قبول/رفض مع رفع إيصال) — admin/payment-requests.php قد يوجد بالفعل؟ فحص
- [ ] test: اشتراك يُفعّل badge + verified_until صحيح + reject
- [ ] subscription request يُنشئ إشعارًا للأدمن؟ (notifications table موجود) — اختياري

### بعد المرحلة 5:
- المرحلة 6: ترقية admin/chats.php (pagination+filters+delete), admin/calls.php (stats), admin/monitoring.php (users today/week/month + %)
- المرحلة 7: flutter analyze + اختبار شامل (test_bundle_final.py 19/19 ✓ + test_appeals3.py 13/13 ✓ + test_privacy_api.py ✓ + test_reports_curl.sh ✓)
- المرحلة 8: commit + push + Render + smoke test (عبر browser_console_exec على nova-wn25.onrender.com)
- المرحلة 9: تقرير نهائي

### ملاحظات مهمة:
- الخادم المحلي: php -S 0.0.0.0:8080 backend/public/router.php من /home/ubuntu/nova_new
- اختباراتنا تعمل فقط من /tmp (test_time_limit.py فيها get_token)
- RATE_LIMIT_REQUESTS=2000 في .env المحلي (development)
- admin panel يعمل على http://localhost:8080/admin/
- last commit GitHub: 86ada88 — لا رفع حتى نهاية الاختبارات
- admin token: POST /admin/otp/login {email: admin@nova-messenger.com, password: 738155861}
- admin pages: admin/*.php تُخدم من router.php تلقائيًا
- schema.sqlite.sql: سطر 461 verified_until، سطر 352-354 plan أعمدة، 592+ payment_requests


## تحديث المرحلة 5 (12:48)
### أُنجِز الآن أيضًا:
1. ✅ backend/controllers/PaymentRequestsController.php جديد: createRequest (POST /subscriptions/request، منع duplicate pending)، uploadReceipt (multipart receipt في ../storage/receipts، 5MB، صور+PDF)، mySubscriptions (GET /subscriptions/my + is_verified + verified_until + payment_requests)، adminIndex/approveRequest/rejectRequest + notifyUser (notifications table).
2. ✅ index.php: require + routes: /subscriptions/my, /subscriptions/request, /subscriptions/request/{id}/upload, /admin/payment-requests, /admin/payment-requests/{id}/approve, /admin/payment-requests/{id}/reject.
3. ✅ admin/payment-requests.php: صفحة كاملة (جدول، قبو/رفض مع ملاحظة، عرض إيصال، modal رفض).

### المتبقي في المرحلة 5:
- [ ] اختبار: curl/Python — request (201/409)، upload، admin approve (يجب أن يفعّل subscription + verified_until + badge + إشعار)، reject، my subscriptions
- [ ] التأكد من أن admin panel يعرض رابط الصفحة في sidebar (admin-panel.php أو sidebar include) — ربما admin-payment-requests.php في sidebar. فحص /home/ubuntu/nova_new/admin/admin-panel.php أو _sidebar

### المرحلة 6 (التالية):
- admin/chats.php: pagination + filters + admin delete + audit
- admin/calls.php: stats (today total, active, audio/video)
- admin/monitoring.php: users today/week/month + %، messages today/yesterday/week
- فحص routes: admin/chats؟ يوجد GET /admin/chats (AdminController messagesIndex?) — فحص index.php "chats"
- admin pages routes: router.php يخدم admin/*.php مباشرة؟ router.php عند خط 19+ يخدم /admin/... — التحقق

### ملاحظات الاختبار (مهم):
- اختبار الباقات: استخدم get_token من /tmp/test_time_limit.py (من /tmp فقط)
- admin token: POST http://localhost:8080/api/v1/admin/otp/login {email,password}
- admin panel sidebar includes: grep -n "payment-requests" admin/admin-panel.php بعد إضافتها
- DB يحتوي plan مجاني؟ فحص: plans الموجودة بعد بذور Render — يوجد عادة free/premium. عند الاختبار استخدم plan موجود


## حالة فورية (12:55) — إصلاح ثغرة أمن في PaymentRequestsController
المشكلة المكتشفة: routes /admin/payment-requests/* كانت تسمح لأي مستخدم عادي بتفعيل اشتراك (admin approve نجح بتوكن مستخدم عادي — 200 "تمت الموافقة وتفعيل الاشتراك").

الإصلاح الجارٍ: إضافة requireAdmin() في PaymentRequestsController (يتحقق payload role=admin + exp) واستدعاؤه في بداية adminIndex/approveRequest/rejectRequest.

ملاحظة مهمة (AuthMiddleware الجديد): أُضيف standalone admin JWT support في AuthMiddleware::authenticate (role=admin في payload → لا فحص sessions). هذا يجعل كل admin routes التي تستخدم AuthMiddleware فقط (مثل /admin/payment-requests في PaymentRequestsController وadmin controllers التي تستخدم adminId()) — لكن AdminController يضيف فحص admins table لاحقًا (خطوة آمنة). PaymentRequestsController يحتاج requireAdmin يدوي.

تفاصيل test_subscriptions.py (/tmp): يختبر 22 فحصًا. آخر نتيجة 19/22: 2 fails = "user cannot approve" (الثغرة) + verified_until (نفس السبب). بعد إضافة requireAdmin: يجب أن يرجع user approve 403.

بعد إصلاح requireAdmin: إعادة تشغيل python3 /tmp/test_subscriptions.py (يجب 22/22).

### الخطوات المتبقية (مرتبة):
1. إضافة $this->requireAdmin(); في adminIndex, approveRequest, rejectRequest في PaymentRequestsController
2. python3 /tmp/test_subscriptions.py حتى 22/22
3. إضافة payment-requests إلى sidebar: فحص admin/admin-panel.php أو ملف sidebar common (grep include/requires في admin/*.php)
4. المرحلة 6: admin/chats.php + admin/calls.php + admin/monitoring.php upgrade
5. اختبار شامل: test_bundle_final.py, test_appeals3.py, test_privacy_api.py, test_reports_curl.sh + flutter analyze
6. مراجعة git diff (لا أسرار)، ثم commit + push + Render deploy + browser smoke test على nova-wn25.onrender.com
7. تقرير نهائي

### حقائق بيئة:
- الخادم المحلي: php -S 0.0.0.0:8080 backend/public/router.php من /home/ubuntu/nova_new
- JWT_SECRET المحلي: من env الساندبوكس (فارغ)
- اختبارنا يعمل فقط من /tmp (import get_token من test_time_limit.py)
- admin: admin@nova-messenger.com / 738155861
- Render: nova-wn25.onrender.com (IP الساندبوكس محجوب → smoke test عبر browser_console_exec)
- DB: backend/config/nova.sqlite — أُعيد بناؤه من schema.sqlite.sql الجديد (12:00 تقريبًا)
- admin panel: php router.php يخدم admin/*.php على /admin/{file}.php
- payment requests seeded plans: id=1 "باقة بريميوم" (100 SAR monthly, paid, enable_verification=1, duration=7)
- users الاختبار: +966509000004(id13,verified بعد approve), 000005(id14), 000006(id40) + مستخدمون عشوائيون من tests


## تحديث المرحلة 6 (13:05) — ترقية لوحة الإدارة
### أُنجز في المرحلة 6 حتى الآن:
1. ✅ test_subscriptions.py: 21/21 PASS
2. ✅ PaymentRequestsController: requireAdmin() مضاف في adminIndex/approveRequest/rejectRequest (ثغرة أمن مغلقة — المستخدم العادي كان يستطيع approve! الآن 403)
3. ✅ sidebar.php: أضيفت روابط appeals.php (⚖ + badge pending) وpayment-requests.php (⟠ + badge pending) بعد البلاغات
4. ✅ admin/appeals.php (جديد ~200 سطر): إحصائيات 3 حالات، جدول اعتراضات مع بيانات المستخدم وسبب الحظر وsuspend_until، قبول (يلغي الحظر + is_blocked=0 + revokes sessions) + رفض مع ملاحظة 500 حرف، modal، audit
5. ✅ admin/chats.php (ترقية كاملة ~260 سطر): فلاتر (type/private-group، q، user، conv_id، msg_id، date range from/to)، pagination (30/صفحة)، حذف إداري للرسائل مع audit_log (deleted_at+deleted_by)، جدول أعضاء المحادثة (?conv=N)، confirm modal

### ملاحظة تقنية chats.php:
- h2() helper معرف في أسفل الملف — PHP 8 لا يسمح باستدعاء دالة معرفة لاحقًا؟ **الخطر**: h2() مُعرّف بعد استخدامه في الكود! PHP يدعم hoisting للدوال ✓ آمن.
- conversation_members أعمدة: user_id, conversation_id, role, left_at (فحصها ✓)
- messages أعمدة: deleted_at, deleted_by موجودة؟ chats.php تستخدمها — يجب التأكد عبر migrateMissingColumns أو التحقق!
- audit_log columns: admin_id, action, target_type, target_id, details — يجب التأكد

### المتبقي في المرحلة 6:
- [ ] التأكد: messages.deleted_at/deleted_by وaudit_log.exists وconversation_members أعمدة صحيحة (php CLI فحص schema)
- [ ] admin/calls.php upgrade: إحصائيات (today total، total today week، audio/video split، active) — حالياً 16 سطر
- [ ] admin/monitoring.php upgrade: users today/week/month + %، messages today/yesterday/week، online %
- [ ] admin/reports.php upgrade: تفاصيل، أولوية، filters، ban/suspend من البلاغ (حالياً 117 سطر — يعرض حالات فقط)
- [ ] admin/subscriptions.php: عرض verified_until (من migrateMissingColumns ✓)
- [ ] اختبار كل الصفحات محليًا: curl/wget http://localhost:8080/admin/... مع admin session cookie (login عبر POST /admin/login.php ثم cookie jar)
- **مهم**: الصفحات محمية بـguardAdmin()/requirePermission — اختبار curl يحتاج cookie jar من login.php
- login admin: POST http://localhost:8080/admin/login.php {email, password} → session cookie

### المرحلة 7 (التالية):
- flutter analyze في nova_flutter (الخصوصية screen أُنجزت ✓ — لكن يجب إعادة فحص بعد أي تغيير؟ لم يتغير flutter منذ المرحلة 3)
- اختبار شامل: test_bundle_final.py (19/19)، test_appeals3.py (13/13)، test_subscriptions.py (21/21)، test_privacy_api.py، test_reports_curl.sh
- git diff review (no secrets)

### المرحلة 8: commit + push + Render deploy + browser smoke test (nova-wn25.onrender.com — IP sandbox محجوب → browser_console_exec)

### حقائق:
- admin token API: POST /api/v1/admin/otp/login → data.token (role=admin standalone JWT)
- لوحة admin panel (PHP): guardAdmin()/requirePermission على الصفحات
- sidebar badges: pendingReports/pendingAppeals/pendingPaymentRequests من DB مباشرة
- last GitHub commit: 86ada88 (لا رفع قبل نجاح الكل)
- test files: /tmp/test_bundle_final.py, /tmp/test_appeals3.py, /tmp/test_subscriptions.py, /tmp/test_privacy_api.py, /tmp/test_reports_curl.sh
- الخادم المحلي: php -S 0.0.0.0:8080 من /home/ubuntu/nova_new (session: default)


## تحديث المرحلة 6 (13:15)
### أُنجز:
1. ✅ database.php: +messages.deleted_by (column) +audit_logs (table في migrateMissingTables)
2. ✅ schema.sqlite.sql: +messages.deleted_by
3. ✅ admin/chats.php: كُتب كاملًا (~180 سطرًا) — فلاتر (type، q، user، conv_id، msg_id، date range)، pagination، حذف إداري (UPDATE messages SET deleted_at + INSERT INTO audit_logs بأعمدة حقيقية: admin_id/action/entity_type/entity_id/description/ip_address)
4. ✅ admin/calls.php: كُتب كاملًا — إحصائيات (today، voice/video اليوم، answered/missed، week، avg duration)، فلاتر (status، type، day)
5. ✅ sidebar.php: روابط appeals.php + payment-requests.php مع badges (pending)

### ملاحظة audit_logs:
- جدول موجود في schema.sqlite.sql (سطر 39-49) ✓ لكن كان غائبًا من DB المحلي → أضف في migrateMissingTables ✓
- الأعمدة الحقيقية: admin_id, action, entity_type, entity_id, description, ip_address, user_agent, created_at

### المتبقي في المرحلة 6:
- [ ] admin/monitoring.php: ترقية — users today/week/month + %، messages today/yesterday/week، online % (الآن 342 سطر — قراءة ثم إضافة)
- [ ] admin/reports.php: ترقية — تفاصيل، أولوية، filters، ban/suspend (الآن 117 سطر)
- [ ] admin/subscriptions.php: عرض verified_until
- [ ] اختبار الصفحات: curl مع cookie jar من login.php (POST /admin/login.php {email,password}) ثم GET كل صفحة (appeals, payment-requests, chats, calls, reports, monitoring, subscriptions, plans)
- [ ] التأكد من عدم وجود أخطاء PHP في الصفحات الجديدة: php -l

### بعدها:
- المرحلة 7: flutter analyze + اختبار شامل (19/19 bundle، 13/13 appeals، 21/21 subscriptions، privacy، reports curl)
- المرحلة 8: commit + push + Render + smoke test (browser_console_exec على nova-wn25.onrender.com)


## تحديث (13:25) — ملاحظات فنية مهمة
- admin/monitoring.php: ✅ أُعيدت كتابته — users today/week/month، messages today/yesterday/week، users_online خلال 5 دقائق، bans_active/appeals_pending/reports_pending/calls_today، إصلاح user_devices→device_registrations (أعمدة os/last_seen)، NULLS LAST، grid4/grid3 CSS inline
- حقائق confirmed من DB المحلي: users أعمدته: id,uuid,phone,email,password_hash,email_verified,phone_verified,name,username,bio,avatar,status_text,is_online=0,last_seen=NULL,is_verified,is_blocked,blocked_at,created_at,updated_at,verified_until (لا deleted_at!)
- reports: id,reporter_id,reported_user_id,message_id,conversation_id,reason,description,status(pending/reviewing/resolved/rejected),reviewed_by,reviewed_at,created_at,reason_code,priority(medium)
- admin/includes/auth.php: requireAdminLogin(), requirePermission($admin, perm), hasPermission, logAudit($admin, action, entityType, entityId, description) → audit_logs(admin_id,action,entity_type,entity_id,description,ip_address,user_agent,created_at) مع NOW() ✓ (MysqlCompatPdo يحوله)، csrfToken/verifyCsrf
- admin/monitoring.php قديم كان يحتوي: stories (غير موجود في DB!) — تم استبداله بـbans/appeals/reports card ✓ (لا مرجع stories متبقٍ)
- admin/calls.php & chats.php & appeals.php & payment-requests.php & monitoring.php كلها php -l نظيفة

### المتبقي الآن:
- [ ] admin/reports.php: ترقية (117 سطر حالي) — تفاصيل البلاغ، الأولوية، message preview، ban/suspend، فلاتر أفضل
- [ ] admin/subscriptions.php: عرض verified_until
- [ ] اختبار الصفحات عبر curl مع cookie jar (POST /admin/login.php {email:admin@nova-messenger.com,password:738155861})
- [ ] المرحلة 7: flutter analyze + اختبار شامل
- [ ] المرحلة 8: commit + push + Render + smoke (browser_console_exec nova-wn25.onrender.com)

### حالة الاختبارات المحلية:
- test_bundle_final.py: 19/19 ✅ (في /tmp)
- test_appeals3.py: 13/13 ✅
- test_subscriptions.py: 21/21 ✅
- test_reports_curl.sh: ✅
- test_privacy_api.py: ✅


## تحديث (13:40) — المرحلة 6 شبه مكتملة
- admin/reports.php: ✅ أعيدت كتابته كاملًا — تفاصيل البلاغ (message preview، attachments، ban status)، ban نهائي + suspend 24h، فلاتر حالة/أولوية، pagination، حالات جديدة action_taken/closed
- admin/monitoring.php: ✅ إحصائيات موسعة (users today/week/month، messages today/yesterday/week، bans/appeals/reports pending، CSS grid4/grid3 inline)
- إصلاح صلاحيات pages: monitoring→settings.view, chats/calls→chats.view, plans→plans.view, subscriptions→subscriptions.view (كانت requirePermission خاطئة)
- getAdminDB() في admin/includes/config.php تستخدم MysqlCompatPdo ✓ (NOW()/IF() يُترجمان)
- login.php يتطلب: _csrf (من GET أولًا) + login field (email) + password
- اختبار admin pages محلي: curl -c jar -L POST /admin/login.php → ثم GET كل الصفحات

### المتبقي:
- [ ] إعادة اختبار جميع صفحات admin بعد إصلاح الصلاحيات
- [ ] إضافة صلاحيات جديدة (chats.*, calls.*, settings.*, users.*) إلى /tmp/seed_permissions.php إن لزم لإعادة البذر على Render
- [ ] admin/subscriptions.php: إضافة verified_until column (عمود التوثيق حتى) — تم فحص الجدول، العمود يظهر ✓ (is_verified فقط حاليًا؛ أضف verified_until بعد "ينتهي في")
- [ ] admin/users.php: ban من users.php يعمل (is_blocked=1 + user_bans + sessions revoke) ✓ موجود
- [ ] المرحلة 7: flutter analyze + اختبار شامل (test_bundle_final 19/19, test_appeals3 13/13, test_subscriptions 21/21, test_privacy_api)
- [ ] مراجعة git diff (أسرار) ثم commit + push + Render deploy
- Render smoke: browser_console_exec على nova-wn25.onrender.com/web_app/ (IP sandbox محجوب مباشرة)
- Commit السابق: 86ada88


## المرحلة 8 — النشر (13:41)
- GitHub: commit b4cf6ed مرفوع على main ✓ (22 ملف، +2506/-255)
- Render deploy نجح: /api/v1/health → 200 ✓ (deploy جديد تلقائي من push)
- Smoke على الإنتاج عبر browser_console_exec على https://nova-wn25.onrender.com/web_app/:
  - health: 200 ✓
  - auth/config: 200 ✓ (phone/email registration/login مفعلان)
  - privacy anon: 401 UNAUTHORIZED ✓ (مطلوب تسجيل دخول)
  - register +9665019998881 prodtest: 200 ✓ (OTP dev يُحفظ للمدير)
  - login لرقم موجود: 200 ✓
- المتبقي: إكمال smoke (verify-otp عبر admin/otp/registrations، messages، privacy after login، admin pages على Render)
- Render DB يُمسح مع كل deploy → يجب إعادة بذر admin + seed_permissions على Render
- Admin prod: admin@nova-messenger.com / 738155861
- get_render_tokens.py في /tmp يجدد توكنات الإنتاج (auth/login → admin otp login → registrations → code → verify-otp)


## نتائج اختبار الإنتاج (Render) — 13:43 ✓
- health: 200 ✓، auth/config: 200 ✓
- register → OTP عبر لوحة التحكم (admin/otp/registrations → code) → verify-otp: يعمل ✓
- /users/me: 200 مع بيانات كاملة ✓، /privacy GET (16 حقل) ✓ + PUT ✓
- /users/search ✓، /plans 200 (count=0 طبيعي — DB نظف مع deploy ولا توجد خطط مبذرة، يمكن إنشاء باقة من plans.php)
- /reports، /appeals: 200 ✓
- لوحة التحكم: تسجيل الدخول يعمل ✓، sidebar يعرض الاعتراضات وطلبات الاشتراك ✓، index.php يعمل بدون أخطاء
- ملاحظة: تسجيل دخول لوحة التحكم على الإنتاج يحتاج CSRF + حقل "login" (مقبول name/email) — عبر المتصفح يعمل مباشرة
- تبقى: بذور باقة افتراضية على الإنتاج اختياري (الخطوات من admin/plans.php)


## المرحلة 8 — التصحيح والرفع النهائي (13:47)

**Bug مكتشف على الإنتاج: Fatal guardAdmin() غير معرّفة في admin/appeals.php** (و payment-requests.php قد يستخدمها؟ لا — grep أظهر appeals.php فقط يستخدم guardAdmin). إصلاح:
- admin/appeals.php: استبدال guardAdmin(); بـ `$admin = requireAdminLogin(); requirePermission($admin, "appeals.view");` ✓
- admin/calls.php: كان 'chats.view' → أصبح 'calls.view' ✓
- database/seed_production.sql: أُضيفت الصلاحيات الجديدة (id 100-110: appeals.view/resolve, users.suspend, payment_requests.view/approve/reject, chats.view, calls.view, chats.admin_delete, plans.view, subscriptions.view) — super_admin يأخذها تلقائيًا عبر SELECT 1,id FROM permissions ✓
- seed_permissions المحلية بُذرت في DB المحلي ✓ (role 1)

**ملاحظة login محلي**: curl login أعاد 200 بدون redirect_url — ربما CSRF extraction فشل (grep لم يجد الـcsrf). عبر المتصفح يعمل. لا يهم للاختبار.

**المتبقي الآن**:
1. اختبار pages المعدلة على الإنتاج عبر المتصفح (appeals.php, payment-requests.php, chats.php, calls.php) — لكن DB الإنتاج نظف → login admin يعمل (seed_production يبذر admin). يجب انتظار deploy الجديد أولًا!
2. git add + commit + push (الإصلاحات الجديدة)
3. انتظار Render build (~2-3 دقائق) ثم اختبار الإنتاج النهائي
4. التقرير النهائي للمستخدم

**روابط**: الإنتاج https://nova-wn25.onrender.com | GitHub alghazaliye/nova | آخر commit قبل الإصلاح: b4cf6ed
**Admin**: admin@nova-messenger.com / 738155861


## المرحلة 8.5 — إصلاحات ما بعد الرفع (04:03 UTC)

**Commits بعد b4cf6ed:**
- 35df10d: appeals.php guardAdmin→requireAdminLogin، calls.php chats.view→calls.view، seed_production +11 perm (100-110)
- 568b9c9: admin/403.php جديدة (مطلوبة من requirePermission في auth.php سطر 48)
- c3c85c3: seed_production — نقل grant super_admin إلى نهاية الملف (بعد INSERT permissions 100-110)
- ae0d58e: إعادة كتابة payment-requests.php — layout موحدة (header/sidebar/footer)، requireAdminLogin + requirePermission payment_requests.view/approve/reject، AJAX عبر localStorage adminToken (footer.php يزرعها)، جدول بإحصائيات 4 بطاقات

**ملاحظة gh**: توكن gh في الـenv قديم (invalid) — استخدمت توكن المستخدم github_pat_ في remote url للدفع ثم نظفتها.

**اختبارات محلية بعد التعديلات**: test_bundle_final 19/19 ✓، test_appeals3 13/13 ✓، test_subscriptions 21/21 ✓، test_privacy_api ✓

**Deploy Render بعد ae0d58e**: health 200 ✓
**الإنتاج بعد الإصلاح**:
- admin login ✓
- appeals.php ✓ (بطاقات 0/0/0 + "لا توجد اعتراضات")
- payment-requests.php ✓ (صفحة جديدة موحدة تعرض، لكن screenshot أظهر الجدول مكررًا مرتين + header/sidebar مفقودين في أول نسخة — النسخة الجديدة ae0d58e يجب أن تكون موحدة؛ الصورة الأخيرة كانت من نسخة قديمة)
- تبقى: اختبار calls.php/chats.php على الإنتاج + التقرير النهائي

**المتبقي**:
1. اختبار calls.php وchats.php على الإنتاج (login admin أولاً)
2. flutter analyze (0 errors سابقًا) — اختياري إن لم يتغير Flutter
3. التقرير النهائي


## نتائج اختبار الإنتاج النهائي (04:04 UTC) — جميع الصفحات تعمل ✅
- login admin ✓ (نفس الجلسة متكررة الاختبار)
- appeals.php ✓ بطاقات إحصائية + جدول "لا توجد اعتراضات"
- payment-requests.php ✓ النسخة الجديدة الموحدة (sidebar + header + 4 بطاقات + جدول + أزرار قبول/رفض)
- calls.php ✓ إحصائيات (0/0/0...) + فلاتر حالات/أنواع/تاريخ + جدول
- chats.php ✓ فلاتر متقدمة (نوع/نص/مستخدم/رقم محادثة/رسالة/تواريخ) + pagination + "1 إجمالي: 0 محادثة"
- index.php (dashboard) ✓

**الخلاصة: كل صفحات الإنتاج تعمل. المتبقي: flutter analyze اختياري + التقرير النهائي.**


## المرحلة النهائية (04:13 UTC) — كل شيء جاهز

**إصلاح Flutter غير متوقع**: flutter analyze كشف error واحد في nova_web_state_web.dart (dart:js_util أزيلت من SDK 3.10) — أُصلح باستخدام dart:js_interop_unsafe indexer + dartify()، ثم أعيد بناء wasm بنجاح (✓ Built build/web)، 0 errors في flutter analyze.

**web_app**: Dockerfile ينسخ web_app من repo (كانت في gitignore) → رُفعت بـ git add -f (commit 68c0376).

**Commits النهائية**: b4cf6ed → 35df10d → c3c85c3 → 568b9c9 → ae0d58e → 68c0376

**Smoke نهائي على الإنتاج**:
- health 200 ✓، web_app/index.html 200 ✓، main.dart.wasm 200 ✓
- التطبيق يفتح على production بدون أخطاء console ✓ (شاشة تسجيل دخول/إنشاء حساب ظاهرة)
- لوحة التحكم: login + appeals ✓ + payment-requests ✓ + calls ✓ + chats ✓ + index ✓

**الاختبارات المحلية النهائية**: test_bundle_final 19/19 ✓، test_appeals3 13/13 ✓، test_subscriptions 21/21 ✓، test_privacy_api ✓، test_reports_curl ✓

**المتبقي فقط: كتابة التقرير النهائي للمستخدم** (PROGRESS_2026-08-21.md موجود كمرجع)


## مهمة جديدة: الوضع الليلي (Dark Mode) لا يظهر — تشخيص وإصلاح

### التشخيص (04:40 UTC):
- `lib/main.dart` سطر 30-34: `buildNovaTheme(Brightness.light, NovaColors.light)` — ثابت light فقط، لا darkTheme ولا themeMode ديناميكي!
- `lib/utils/nova_ui.dart`: ألوان dark موجودة وجاهزة (NovaColors.dark) + `buildNovaTheme(brightness, colors)` يدعم Brightness.dark ✓
- لا يوجد أي كود dark mode في settings_screen أو providers — يجب إضافته من الصفر
- `auth_provider.dart` يستخدم SharedPreferences بنفس نمط saveToken/loadToken (سطر 208) ✓ shared_preferences ^2.3.2 موجود

### الحل المنفذ:
1. أنشأت `lib/providers/theme_provider.dart` — ThemeProvider ChangeNotifier مع SharedPreferences key='nova_dark_mode'
2. المتبقي:
   - main.dart: استيراد ThemeProvider + MultiProvider(Auth+Theme) + theme=light/darkTheme + themeMode=themeProvider.isDark ? dark : light
   - settings_screen.dart: إضافة عنصر تبديل الوضع الليلي في الإعدادات
   - flutter analyze + flutter build web --wasm + cp build/web → web_app + commit + push + انتظار Render

### خطوات الرفع (نفس النمط السابق):
git add -f web_app/ + git remote set-url origin https://alghazaliye:TOKEN@github.com/... && git push && remote reset
TOKEN: [GITHUB_PAT_REDACTED]

### ملاحظة سابقة للمستخدم: أخبرته أن التغيرات موجودة على الإنتاج والسبب المحتمل كاش المتصفح (Ctrl+Shift+R أو Incognito)


## مهمة الوضع الليلي — التحديث (04:42 UTC)

### أُنجز:
- أنشأت `lib/providers/theme_provider.dart` (SharedPreferences key='nova_dark_mode')
- main.dart: MultiProvider(Auth+Theme) + darkTheme + themeMode ديناميكي
- settings_screen.dart: عنصر "الوضع الليلي" مع NovaSwitch فوق عنصر الخصوصية (سطور 301-312)
- flutter analyze: 0 errors
- flutter build web --wasm: نجح ✓ — النص "الوضع الليلي" موجود في wasm الجديد (grep ✓)
- commit 72f2809 مرفوع على GitHub ✓ (8 ملفات، web_app بـ-f)
- Render deploy نجح: health 200، wasm الجديد على الإنتاج بصمة "الوضع الليلي" ✓
- التطبيق يفتح على الإنتاج (شاشة مرحبًا بك) ✓

### المتبقي:
- اختبار الوضع الليلي بصريًا: النقر على شاشة WASM صعب (canvas) — أحتاج تسجيل دخول تجريبي ثم فتح الإعدادات وتفعيل التبديل. استخدام browser_console_exec dispatch events على flutter-view لم يتجاوب عند (446,429). ربما إحداثيات خاطئة — العنصر "تسجيل الدخول" يمين الشاشة (RTL) عند تقريبًا x=440,y=429 من صورة 893x768.
- بعد الاختبار: التقرير للمستخدم أن الوضع الليلي مضاف في الإعدادات.

### ملاحظة مهمة للإبلاغ:
المستخدم قال "لم تظهر التغيرات" سابقًا — السبب كاش المتصفح. الوضع الليلي هو تغيّر جديد مضاف الآن.


## مشاكل جديدة أبلغ عنها المستخدم (21 Aug)
1. **الرسائل بين مستخدمين لا تصل** — سويت مستخدمين، أرسلت من واحد للآخر، الرسالة لا تصل. يجب تشخيص API رسائل (fetch + send) محليًا.
2. **إعدادات الخصوصية في التطبيق لا تعمل** — يجب تشخيص privacy_screen.dart + API /privacy.
3. **الوضع الليلي لا يظهر** — تم إنجازه كودًا ورفع (commit 72f2809) لكن المستخدم لم يرَه بعد (كاش المتصفح على الأرجح).

### حقائق مفيدة عن Flutter web token:
- ApiService.token يحفظ في SharedPreferences key='token' (وليس flutter.user_id). بدون token يبقى على welcome screen.
- إنشاء حساب تجريبي عبر API: المسار /api/v1/auth/register-phone → NOT_FOUND؟! فحص index.php routes — ربما /register. المستخدمون الحاليون: flutter.user_id=33 (local) موجود.
- canvas Flutter web 1280x1100 بينما CSS 893x768 (ratio ≈1.433) — للتحويل: canvas = css*ratio.

### الخادم المحلي: يعمل على :8080 (php -S) ✓
### خطة تشخيص الرسائل:
- اختبار كامل: register 2 users (curl/JS) → login → conversations create → message send → recipient fetch messages.
- فحص MessagesController + ConversationsController + messages table.


## حقائق API مهمة اكتشفت أثناء اختبار الرسائل (محليًا :8080)
- التسجيل: POST /auth/register {phone} → 200 "تم إرسال رمز التحقق" — cooldown 60s موجود في otp_verifications (status pending/sent/manual → يمنع إعادة إرسال جديد). OTP اليدوي في otp_verifications.manual_code.
- verification: POST /auth/verify-otp {phone, otp} → data.token + data.user.id.
- البحث عن مستخدمين: GET /users/search?q=XX (يجب حرفين على الأقل) — المستخدمان الجديدان ظهرا فقط بعد وجود contact! (u2 ظهر عندما q بطولين). **ملاحظة: التطبيق يستخدم search لعرض جهات الاتصال — قد يكون سبب مشكلة "اختيار مستخدم" لدى المستخدم.**
- إنشاء محادثة: POST /conversations {user_id} (وليس contact_user_id!) → 201 + data.id. يعيد استخدام المحادثة الموجودة (JOIN conversation_members) ✓.
- جدول conversations: لا يوجد user1/user2 — الربط عبر conversation_members (user_id, role, left_at).
- إرسال رسالة: POST /conversations/{id}/messages يتطلب {client_message_id, content, type} — client_message_id إلزامي (UUID فريد)! type: text/image/video/file/voice/location.
- جلب الرسائل: GET /conversations/{id}/messages → data.messages.
- DB المحلي: /home/ubuntu/nova_new/backend/config/nova.sqlite
- رسائل test: /tmp/test_messages2.py (يعمل)


## نتيجة اختبار الرسائل على API المحلي: PASS كامل (3/3)
- تسجيل + OTP + محادثة + إرسال (client_message_id+body) + جلب من الطرفين: يعمل تمامًا.
- المشكلة إذن في Flutter client: كيف يرسل Flutter الرسائل؟ يجب فحص ApiService.sendMessage / MessageService — هل يرسل content بدل body؟ وهل يولّد client_message_id؟ وهل /users/search يعمل بشكل صحيح في شاشة جهات الاتصال (يجب حرفين على الأقل + المستخدم يظهر فقط إن وجدت contacts؟ لا — search يعمل على users مباشرة).
- نقطة مهمة للمستخدم: "ولت مرسلة المستخدم الاخر لم يعامل" — غالبًا السبب: فشل sendMessage في Flutter (422/400) بسبب صيغة body خاطئة (content بدل body)، أو فشل إنشاء المحادثة.

## تحليل Flutter للرسائل
Flutter code سليم: _sendMessage يرسل {client_message_id, type, body} ✓، _openChatWithUser يرسل {user_id} ✓، _searchAndStart يستخدم /users/search?q (min 2 chars) ✓. API محلي يختبر PASS كامل. إذن سبب شكوى المستخدم قد يكون: (1) الإنتاج — يجب اختبار نفس التدفق على Render، (2) أو رسائله لم تصل بسبب poller/WS لا يتحدث، (3) أو أنه لم يجد المستخدم الآخر (search يحتاج حرفين والاسم مختلف). الخطوة التالية: اختبار التدفق كاملًا على الإنتاج (nova-wn25.onrender.com) عبر fetch في المتصفح بمستخدمين حقيقيين، ثم فحص privacy_screen.


## تشخيص privacy API (كامل وسليم)
PUT /privacy يدعم 16 حقلًا: last_seen_visibility, online_status, photo_visibility, status_visibility, phone_visibility, email_visibility, read_receipts, messages_from, calls_from, groups_from, find_by_phone/email/username, display_identity, story_privacy, allow_by_phone — كل visibility يقبل everybody/contacts/nobody ويُخزن int (1,2,3). GET /privacy يعيد 16 حقلًا صحيحًا. INSERT privacy_settings إن لم يوجد + UPDATE. لا مشكلة في الـAPI.

## تشخيص إرسال الرسائل (كامل وسليم)
Flutter chat_screen.dart _sendMessage يرسل {client_message_id, type, body, reply_to_message_id} ✓ — نفس ما يقبله MessageController.send. _openChatWithUser {user_id} ✓. _searchAndStart: /users/search?q مع q.length<2 إلغاء ✓. الاختبار المحلي /tmp/test_messages2.py: 3/3 PASS (تسجيل، محادثة، إرسال، رد من الطرفين).

## المشكلة المتبقية — المطلوب التحقق:
1. **الخصوصية في التطبيق**: privacy_screen.dart قد لا يقرأ/يحفظ بشكل صحيح — أحتاج فحص privacy_screen.dart (معدل سابقًا في هذه الجلسة، 6 أقسام). فحص: هل يقرأ GET /privacy عند الفتح؟ هل PUT يعمل؟ هل showToast عند النجاح؟
2. **الرسائل لا تصل**: API سليم → فحص التوقيت الحقيقي: هل poller/WS في التطبيق يجلب الرسائل؟ هل مشكلة المستخدم على الإنتاج (Render)؟ يجب اختبار تدفق كامل من web_app على الإنتاج (تسجيل + إنشاء محادثة + رسالة + جلب). التطبيق على الإنتاج ما زال يحمل WASM (Render sleep) — انتظر.
3. **الوضع الليلي**: theme_provider.dart أُنشئ + main.dart يعدّل + settings_screen toggle. لم يُختبر بصريًا بعد. الـbuild الجديد مرفوع (commit 68c0376 أو بعده).

## بيانات اختبار:
- المستخدمون التجريبيون المستخدمون في الاختبارات محليًا: +966770911001 (id=67), +966770911002 (id=68), محادثة id=16، رسائل id=22,23.
- الخادم المحلي :8080 يعمل (php -S public/router.php).
- الإنتاج: https://nova-wn25.onrender.com (Render sleep ~20-40s عند الاستيقاظ).


# مهام جديدة من المستخدم (21 أغسطس 2026 — جلسة متأخرة)

## ملاحظة مهمة: المستخدم أرسل في نفس الرسالة:
1. (رسالة نصية مباشرة): مراجعة وإصلاح نظام "آخر ظهور" بشكل كامل (15 نقطة) + اختبارات الحالات
2. (ملف pasted_content_21.txt): تطوير نظام "الحالات Status" كاملًا كنظام WhatsApp Status (49 قسم)

## قائمة المهام الجديدة:

### أ) نظام آخر ظهور (مهم جدًا):
1. حفظ last_seen حقيقي في DB (users.last_seen موجود)
2. تحديث عند خروج/Offline (heartbeat + timeout)
3. حالة "متصل الآن" حقيقية (online)
4. صيغ عرض: متصل الآن / منذ 5 دقائق / منذ ساعة / أمس / منذ يومين / منذ 3 أيام / تاريخ محدد
5. وقت حقيقي + timezone (Asia/Riyadh)
6. بدون استهلاك زائد (heartbeat أثناء استخدام فقط)
7. WebSocket إن وجد وإلا آلية مناسبة
8. Heartbeat/Timeout عند انقطاع (لا يظهر متصل خطأ)
9. Offline بعد انتهاء مهلة
10. Privacy: الإعدادات → الخصوصية → آخر ظهور (الجميع / جهات الاتصال / لا أحد)
11. تطبيق الخصوصية في Backend (API لا يعيد last_seen لمن لا يملك صلاحية)
12. لا كسر للنظام الحالي

### ب) نظام الحالات (pasted_content_21.txt — 1143 سطر):
- إنشاء حالة (نص/صورة/فيديو) + 24 ساعة + expires_at
- حالات متعددة لمستخدم واحد (تظهر مرة واحدة + progress bars)
- عرض: avatar/اسم/وقت + "حالة جديدة" / "المشاهدات" أقسام
- Progress bar: tap يمين/يسار، ضغط مطول pause، سحب لإغلاق
- تسجيل المشاهدات UNIQUE(status_id, viewer_id) في status_views — Backend فقط
- منع تكرار المشاهدات + الأجهزة المتعددة تحسب مرة
- قائمة المشاهدين لصاحب الحالة فقط
- خصوصية الحالات: الجميع / جهات اتصالي / باستثناء / المشاركة مع / لا أحد + خيار "من لديه رقمي"
- الحظر يمنع رؤية الحالات (Backend)
- احترام privacy آخر ظهور + read receipts
- الرد على الحالة (status_id → يصل في المحادثة)
- Reactions: ❤️😂😮😢👍🔥 (UNIQUE per user)
- حذف/تعديل لصاحب الحالة فقط (deleted / admin_deleted)
- Admin: صلاحيات statuses.view/delete/review/stats + admin/statuses.php + audit
- بلاغات على الحالات (status_id → reports)
- FCM عند reply/reaction
- APIs: GET/POST /statuses, GET /statuses/{id}, POST /statuses/{id}/view, GET /statuses/{id}/views, PUT/DELETE /statuses/{id}, POST /statuses/{id}/reply, POST /statuses/{id}/reaction
- Tables: statuses (id,user_id,type,text,media_id,created_at,expires_at,privacy,status,views_count) + status_views + status_reactions + status_replies
- اختبارات: 40 حالة إلزامية
- التنفيذ: محلي أولًا ثم flutter analyze ثم commit ثم Render ثم smoke ثم تقرير

## حالة المشاكل السابقة (لم تكتمل بعد):
- الوضع الليلي: كود Flutter جاهز (theme_provider.dart + settings_screen) — مرفوع للـGitHub ✓ لكن لم يُختبر بصريًا نهائيًا
- الخصوصية في التطبيق: API يعمل ✓، لكن Flutter privacy_screen غير مؤكد عمليًا
- الرسائل بين مستخدمين: API كامل ✓ (test_bundle 19/19) — المشكلة محتملة في Flutter UI
- اختبار الإنتاج test_prod_flow.py: OTP code = None — الاستجابة تستخدم 'data.otp_code' — سكربت مصحح لم يُعاد تشغيله

## حقائق API مهمة (مثبتة):
- register phone: POST /auth/register {phone} → 200
- GET /admin/otp/registrations → {"rows":[{id, phone_number, ...}]}
- GET /admin/otp/registrations/{id}/code → {"data":{"otp_code":"...", "expires_at":"..."}}
- POST /auth/verify-otp {phone, otp} → token
- POST /conversations {user_id} → conv_id
- POST /conversations/{id}/messages → body: {client_message_id, type:'text', body:'...'} (وليس content!)
- GET /privacy → 16+ حقل؛ PUT /privacy → حفظ
- admin login API: POST /admin/otp/login {email, password}

## ملفات رئيسية معروفة:
- Flutter: /home/ubuntu/nova_new/nova_flutter
- Backend: /home/ubuntu/nova_new/backend
- DB: /home/ubuntu/nova_new/backend/config/nova.sqlite
- اختبار messages محلي: /tmp/test_messages2.py (✓)
- اختبار prod: /tmp/test_prod_flow.py (مصحح — يحتاج إعادة تشغيل)
- Admin token prod: /tmp/prod_admin_token.txt


## فحص النظام الحالي (المرحلة 1 — حالة ما قبل التنفيذ)

### آخر ظهور — الحالة الحالية:
- **DB**: users فيها is_online + last_seen ✓ + indexes
- **Backend**: AuthController logout يضبط is_online=0 + last_seen=NOW() (سطر 420). UserController.php: online endpoint (سطر 202) يضبط is_online=1 + last_seen. getUser يعيد last_seen للمشاهد نفسه فقط إذا viewer==id (سطر 118-123). search يعيد is_online+last_seen بدون فلترة خصوصية! (سطر 149)
- **privacy**: privacy_settings فيها show_last_seen + show_online_status (UserController سطر 215-260: last_seen_visibility visibility=0/1/2)
- **Flutter**: formatLastSeen في lib/models/user_model.dart (سطر 259-286) — صيغة كاملة جيدة (منذ لحظات/دقيقة/ساعة/يوم/تاريخ عربي، utcOffsetMinutes من AuthProvider)
- **مفقود**: لا يوجد heartbeat حقيقي/تلقائي في Flutter (لا يوجد كلمة heartbeat/online في services/providers). is_online يُحدَّث فقط عند تسجيل دخول أولي. آخر ظهور لا يتحدث عند خروج/خلفية/انقطاع.
- chats_screen.dart يستخدم formatLastSeen عند 988 و1767؛ chat_screen عند 1644.

### الحالات (Stories) — الحالة الحالية:
- **DB**: stories (id, uuid, user_id, type text/image/video, text, file_id→attachments, privacy default 'contacts', created_at, expires_at, deleted_at) + story_views (UNIQUE story_id+viewer_id) ✓
- **Backend**: StoryController.php كامل (262 سطر): index/create/upload/show/view/delete + sendStoryNotifications (FCM)
- routes موجودة: GET/POST /stories، GET/POST/DELETE /stories/{id}، /stories/{id}/upload
- **Privacy الحالي**: all/contacts/close_friends فقط — المطلوب: everyone/contacts/exclude_contacts/only_contacts/none + حجب المحظورين
- **Flutter**: stories_screen.dart (492 سطر) + story_viewer_fullscreen.dart + web_story_video.dart موجودة — نظام حالات موجود جزئيًا من قبل
- **مفقود من المواصفات**: reactions + replies + excluded/allowed lists + block check في index + views list لصاحب الحالة + admin/statuses.php + صلاحيات statuses.*

### خلاصة الاستراتيجية:
1. **آخر ظهور**: إصلاح صغير نسبيًا — (أ) privacy في backend لكل endpoints تعيد last_seen (search + getUser + contacts)، (ب) heartbeat تلقائي في Flutter (Timer 60s عند foreground + تحديث عند resume + page visibility)، (ج) offline عند logout/crash (visibilityChange)
2. **الحالات**: ترقية StoryController (privacy expanded + blocked check + views endpoint لصاحب الحالة + reactions + replies) + Flutter stories_screen upgrade + reactions/replies UI + admin/statuses.php
3. المواصفات الكاملة في pasted_content_21.txt (يجب إعادة قراءته عند التنفيذ)


## تشخيص آخر ظهور — الفجوات (سطر 300+)

النظام الأساسي موجود (is_online/last_seen + canSeeLastSeen/canSeeOnline + filterProfile) لكن **غير مطبّق في كل الأماكن التي تعرض آخر ظهور**:

| Endpoint/مكان | الحالة | المشكلة |
| --- | --- | --- |
| GET /users/search | يعيد is_online+last_seen للجميع | لا فلترة canSeeLastSeen/canSeeOnline |
| ConversationController.getOtherParticipant | يعيد is_online+last_seen مباشر | لا فلترة خصوصية |
| GroupsController members | يعيد last_seen مباشرة | لا فلترة خصوصية |
| GET /heartbeat | POST يضبط is_online=1 ✓ | لا يوجد **offline heartbeat** ولا آلية حضور/غياب (لا أحد يضبط is_online=0 إلا logout) |
| Flutter | formatLastSeen كامل ✓ | لا يوجد heartbeat تلقائي في التطبيق — لا timer ولا WidgetsBindingObserver |

### خطة التنفيذ (آخر ظهور):
1. **Backend**:
   - GET /users/search: تطبيق canSeeLastSeen/canSeeOnline لكل صف (if !$show → last_seen=null, is_online=false)
   - ConversationController.getOtherParticipant: تطبيق الفلترة (استدعاء UserController::canSee*)
   - GroupsController members: تطبيق الفلترة
   - **POST /heartbeat/offline** جديد: يضبط is_online=0, last_seen=NOW()
   - **Timeout تلقائي**: عند heartbeat: إذا last_seen أقدم من 5 دقائق → is_online=0 (أو cron-like: في كل heartbeat نحدّث الآخرين المتأخرين؟ الأفضل: عند جلب is_online في أي read — CASE WHEN last_seen < NOW()-INTERVAL 5 MINUTE THEN 0)
   - الأفضل: دالة مساعدة isOnlineExpr() في MysqlCompatPdo — تعديل SELECTs لتعرض is_online=(is_online AND last_seen > last 5 min) + last_seen يظل كما هو
2. **Flutter**:
   - إضافة heartbeat timer (كل 45s عند foreground فقط) + WidgetsBindingObserver: AppLifecycleState.resumed → heartbeat، paused → offline
   - window visibility listener (web): blur → offline
   - إرسال POST /heartbeat عند تسجيل الدخول وكل 45s + offline عند logout
3. **Privacy UI**: privacy_screen.dart — حقل "آخر ظهور" يجب أن يوجد (last_seen_visibility: الجميع/جهات الاتصال/لا أحد) — التحقق أن الإعداد يُرسل backend (privacyUpdate يدعمه ✓ UserController سطر 260 last_seen_visibility→show_last_seen)

### ملاحظة timezone: dates تُخزن UTC ('+00:00') في SQLite localtime()؟ — last_seen يُخزن بـ NOW() الذي يُترجم لـ datetime('now','localtime')! يعني يخزن localtime الساندبوكس وليس UTC. لكن Flutter parse + utcOffsetMinutes يعالج. على Render localtime = UTC (السيرفر). لا مشكلة عملية.


## تقدم تنفيذ آخر ظهور (المرحلة 2 — قيد التنفيذ)

### أُنجز في Backend:
- ✅ UserController.php: أضفت `applyPresencePrivacy(array $row, int $viewerId)` — تطبق canSeeOnline/canSeeLastSeen + مهلة حضور 300 ثانية (is_online→false بعد 5 دقائق بدون نبضات)
- ✅ search() يستخدم applyPresencePrivacy لكل صف ✓

### المتبقي في Backend:
1. ConversationController: getOtherParticipant — استدعاء UserController::applyPresencePrivacy عليها. **ملاحظة: ConversationController لا يحتوي require UserController** — يجب require_once backend/controllers/UserController.php + إضافة private method wrapper أو استدعاء (new UserController())->applyPresencePrivacy
2. GroupsController members (سطر 78): نفس الفكرة — لكن controller لا require UserController أيضًا. استخدم require_once + new UserController()
3. **heartbeat timeout جماعي**: إضافة منطق "تنظيف" — عند كل heartbeat(): UPDATE users SET is_online=0 WHERE is_online=1 AND last_seen < NOW() - INTERVAL 5 MINUTE (MysqlCompatPdo يحول ✓). هذا يضمن عدم بقاء أحد "متصل" إلى الأبد
4. Flutter heartbeat timer (45s foreground) + offline عند pause/logout + visibility blur (web)

### Heartbeat timeout SQL (للاستخدام في heartbeat()):
```sql
UPDATE users SET is_online = 0 WHERE is_online = 1 AND last_seen < NOW() - INTERVAL 5 MINUTE
```

### ConversationController.getOtherParticipant (سطر 328-337):
SELECT u.id, u.uuid, u.name, u.username, u.avatar, u.is_online, u.last_seen, u.is_verified FROM conversation_members cm JOIN users u... WHERE cm.conversation_id=? AND cm.user_id!=?

### GroupsController members (سطر 75-83):
SELECT cm.user_id, cm.role, cm.joined_at, u.name, u.avatar, u.username, u.phone, u.last_seen FROM conversation_members cm JOIN users u...

### Flutter plan (الموجود):
- chats_screen.dart سطر 988: formatLastSeen(u['last_seen'], isOnline: online) — online يأتي من u['is_online']
- chat_screen سطر 1644: widget.conv.isOnline + widget.conv.lastSeen
- chats_screen 1767: نفس pattern
- إضافة: provider جديد أو timer في main/home screen: POST /heartbeat كل 45s (foreground فقط)، offline عند pause، offline عند logout
- Flutter offline detection: WidgetsBindingObserver + Timer.periodic (web + mobile)


## تقدم المرحلة 2 — Backend مكتمل تقريبًا

### أُنجز في Backend (كله في nova_new/backend/controllers/):
1. ✅ UserController.applyPresencePrivacy($row, $viewerId) — سطر ~508: مهلة حضور 300s + canSeeOnline + canSeeLastSeen (nobody يخفي الاثنين)
2. ✅ UserController.search() يستخدمها
3. ✅ UserController.getPublicProfile() يستخدمها قبل filterProfile
4. ✅ UserController.heartbeat(): cleanup جماعي (is_online=0 لمن last_seen < NOW()-5min) ثم online+last_seen للـuser
5. ✅ ConversationController.getOtherParticipant(): require_once + new UserController + applyPresencePrivacy
6. ✅ GroupsController members: require_once + applyPresencePrivacy لكل عضو

### Privacy UI (Flutter): موجودة كاملة — privacy_screen.dart: last_seen_visibility key ✓ مع options everybody/contacts/nobody ✓ و_save = PUT /privacy {last_seen_visibility: value} ✓ وbackend privacyUpdate map يقبله ✓ (سطر 266)

### المتبقي للمرحلة 2 (Flutter heartbeat/offline):
- إضافة PresenceService أو timer في home/chats_screen:
  - POST /heartbeat كل 45 ثانية (foreground فقط)
  - WidgetsBindingObserver: resumed → heartbeat فوري، paused/inactive → POST /heartbeat/offline (سأضيف endpoint)
  - (web فقط) window blur/focus → offline/online
- إضافة endpoint: POST /heartbeat/offline → UPDATE users SET is_online=0, last_seen=NOW()
- route في index.php: $router->post('/heartbeat/offline', fn() => (new UserController())->setOffline())
- ملاحظة: flutter web: يجب إضافة listener عبر dart:html أو window.onBlur في platform web file (lib/utils/nova_web_state_web.dart موجود فيه بالفعل window events!) — يمكن إضافة onBlur هناك أو في widget مستقل

### مهم (موجود في nova_web_state_web.dart): window events موجودة (scroll/resize) — يمكن إعادة استخدامها
### route heartbeat موجود في index.php: grep -n "heartbeat" backend/public/index.php للتأكد


## المرحلة 2 مكتملة ✅ (Backend آخر ظهور):
- UserController.setOffline() (POST /heartbeat/offline): is_online=0 + last_seen=NOW()
- index.php: route /heartbeat/offline مضاف ✓
- heartbeat() cleanup جماعي 5 دقائق ✓ + applyPresencePrivacy في search/getPublicProfile/Conversation.getOtherParticipant/Groups members ✓

### المتبقي للمرحلة 2 (Flutter):
- home/chats_screen: إضافة Presence heartbeat timer (كل 45s POST /heartbeat عند resumed/active) + WidgetsBindingObserver paused→POST /heartbeat/offline
- web: nova_web_state_web.dart — إضافة window.onblur→offline + onfocus→online (يوجد _window بالفعل)


## المرحلة 2 — Flutter أُنجز:
- chats_screen.dart: _ChatsTabState مع WidgetsBindingObserver + didChangeAppLifecycleState (resumed→/heartbeat, paused/inactive/detached→/heartbeat/offline) + heartbeat كل 45s + dispose removeObserver ✓
- nova_web_state_web.dart: enablePresenceListenersImpl() — window blur/focus/beforeunload → fetch /heartbeat/offline|online + NovaWebState.enablePresenceListeners() ✓
- المتبقي: استدعاء NovaWebState.enablePresenceListeners() بعد تسجيل الدخول — الأفضل في ApiService.saveToken أو main.dart عند initState للـHomePage. سأضيفه في chats_screen initState (kIsWeb → enablePresenceListeners)

## ملاحظة token للـweb presence: fetch يستخدم window.__novaToken — يجب التحقق أن ApiService يحفظ token بهذا الاسم! grep -n "__novaToken" nova_flutter/
## ثم: flutter analyze + web build + (المرحلة 3: حالات Backend)
