# تشخيص مشكلة الحساب الجديد (2026-08-21)

## ما أُجري
1. بحث المستخدمون على Render عبر /api/v1/users/search — **يعمل** ويرجع id=3,4,5 (مستخدمي NOVA) وid=1,2 (seed أحمد/سارة من seed_production.sql)
2. إرسال رسالة بين الحسابين — **يعمل** (201 + تظهر عند المستقبل)
3. إنشاء محادثة — يعمل
4. الاتصال الصوتي (POST /calls initiate) — يعمل (201، status=ringing)
5. GET /users/4 من حساب آخر — يعمل
6. بحث باسم 'مستخدم' يرجع id=4,5؛ بحث '+9667' يرجع id=4,5
7. بحث '861' يرجع فارغًا — لأن بحث LIKE على phone بـ'%861%' لم يُطابق (البحث '+9667' نجح؛ بحث '861' فقط فشل؟ لا — '+9667' نجح لوجود 4,5 بهاتف +966738155861/+966770105284؛ '861' يجب أن يطابق +966738155861! فشل — سبب محتمل: حد mb_strlen>=2 لكن النتيجة فارغة لأن id=3 مُستثنى (is self) ولا يوجد مستخدم آخر يحتوي 861)

## النتيجة
- **الخادم سليم**: الرسائل والمكالمات والمحادثات تعمل بين الحسابات على Render
- الحسابات الحالية: id=3 (+966738155861), id=4 (+966770105284), id=5 (+966770123456 اختبار)
- الحسابات seed (id=1 أحمد، id=2 سارة) لا تزال موجودة — يجب حذفها لأنها وهمية

## فرضيات لسبب مشكلة المستخدم
1. **حسابه الجديد**: إذا سجّله على التطبيق ولم يُفعَّل (is_verified=0) — بعض endpoints قد ترفضه. لكن في اختبارنا الحساب الجديد id=5 عمل بشكل كامل رغم is_verified=0
2. **مستخدمون سابقون مفقودون**: كل rebuild جديد لـ Render **يمسح DB بالكامل** (بدون Persistent Disk). أي حسابات أُنشئت في rebuild سابق فُقدت. الحساب الوحيد الباقي هو ما أُنشئ بعد آخر rebuild
3. **الذاكرة المؤقتة في التطبيق** (local_nova_db/drift): قد تعرض محادثات قديمة أو تستبعد محادثات جديدة
4. **المكالمات**: تعتمد على WebRTC signaling + polling في التطبيق — إذا التطبيق قديم (cache) أو WebSocket غير مدعوم، قد لا تصل إشعارات المكالمات

## توصيات قواعد البيانات الخارجية المجانية
- Neon (PostgreSQL): free 0.5GB، serverless
- Supabase: free PostgreSQL 500MB + ميزات realtime
- Turso (SQLite عبر HTTP): free 9GB
- PlanetScale: MySQL serverless — free plan توقف (2024)
- TiDB (MySQL-compatible): free 5GB — مشروع Nova يستخدم TiDB في web-db-user
- Aiven: free PostgreSQL small
- ملاحظة: المشروع PHP/SQLite حاليًا — التحول لـPostgreSQL يحتاج تعديل PDO/driver. الحل الأبسط على Render: Persistent Disk (~7$/شهر) يحفظ DB الحالية دون تغيير الكود
